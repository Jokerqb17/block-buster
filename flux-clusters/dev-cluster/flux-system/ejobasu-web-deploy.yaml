apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: nginx-app
  name: web-app
  namespace: wave2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web-app
  template:
    metadata:
      labels:
        app: web-app
    spec:
      containers:
      - image: caddy:latest
        name: webcont
        resources:
          requests:
            cpu: "200m"
